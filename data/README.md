# PLZ-Daten

`plz-de.js` enthält eine kompakte lokale PLZ-Ort-Datenbank für Deutschland.

Quelle: OpenGeoDB über https://ratopi.github.io/opengeodb/

Verwendete Rohdateien:
- `PLZ.tab`
- `DE.tab`

Die Datei wird im Browser vor `script.js` geladen und stellt `window.DE_POSTAL_CODES` bereit.
